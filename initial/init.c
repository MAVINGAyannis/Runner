/*
** EPITECH PROJECT, 2019
** my_runner
** File description:
** C Graphical Programming Module
*/
#include "../my_runner.h"

void init (t_t *texture, s_s *sprite, v_v *vector)
{
    init_texture(texture);
    init_texture_2(texture);
    init_sprite(sprite);
    init_vector(vector);
    init_vector_2(vector);
}
